<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script
      src="https://kit.fontawesome.com/a68fe6556b.js"
      crossorigin="anonymous"
    ></script>
    <link
      rel="stylesheet"
      href="{{ url_for('static', filename='css/output.css') }}"
    />
    <title>Dad's Playgroup {% block title %}{% endblock %}</title>
  </head>
  <body class="bg-neutral-100">
    <header class="bg-white border-b">
      <nav class="container flex items-center gap-6 py-6 mx-auto">
        <a href="/" class="mr-auto text-xl font-semibold text-red-600 uppercase"
          >Dad's Playgroup</a
        >
        <ul class="flex gap-6">
          <li>
            <a href="/#events">Events</a>
          </li>
          {% if session.user %}
          <li>
            <a href="/members">Members</a>
          </li>
          {% endif %}
        </ul>
        <div class="flex items-center gap-6">
          {% if session.user %}
          <a href="/members/{{ session.user.id }}">
            {{ session.user.name }}
          </a>
          <a href="/logout" class="block px-4 py-2 font-medium text-gray-700 border rounded-md shadow-sm"
            >Logout</a
          >
          {% else %}
          <a href="/login" class="block px-4 py-2 font-medium text-gray-700 border rounded-md shadow-sm"
            >Login</a
          >
          <a href="/signup" class="block px-4 py-2 font-medium text-gray-700 border rounded-md shadow-sm"
            >Sign up</a
          >
          {% endif %}
        </div>
      </nav>
    </header>
    <main>{% block content %}{% endblock %}</main>
    <footer
      class="container flex items-center justify-between py-6 mx-auto mt-10 text-sm text-gray-600 border-t"
    >
      <p>&copy; Dad's Playgroup 2023</p>
      <nav>
        <ul class="flex gap-4">
          <li><a href="/">Home</a></li>
          <li><a href="/events">Events</a></li>
          {% if session.user %}
          <li><a href="/members">Members</a></li>
          {% endif %}
        </ul>
      </nav>
    </footer>
  </body>
</html>
